{
  "name": "dagbok-root",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "frontend": "npm run frontend --prefix frontend",
    "backend": "npm run backend --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}